import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class EditService extends Component {
    render() {
        return (
            <div className="edit-service"></div>
        );
    }
}